name:  Shell Commands
on: [push]
jobs:
  run-Shell-Commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Work Action"
  run-Shell-Commands2:
    runs-on: ubuntu-latest
    steps:
      - name: Multiple Lines
        run: |
            node -v
            npm  -v

